<%= form_with(model: giaohang, local: true) do |form| %>
  <% if giaohang.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(giaohang.errors.count, "error") %> prohibited this giaohang from being saved:</h2>

      <ul>
        <% giaohang.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :ngaygiao %>
    <%= form.date_select :ngaygiao %>
  </div>

  <div class="field">
    <%= form.label :tinhtrang %>
    <%= form.check_box :tinhtrang %>
  </div>

  <div class="field">
    <%= form.label :ghichu %>
    <%= form.text_field :ghichu %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
